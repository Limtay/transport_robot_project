# result/ — 결과 문서

현재 유효한 결과만 여기 둔다. 2026-08-21 이전 것은 `../_old/result/` 로 옮겼다.

| 문서 | 내용 |
|------|------|
| `Stage1b_loadcell_recal_260821.md` | **현재 유효한 로드셀 캘리** — 앰프 span 조절 후 재확정 |

## `_old/result/` 에 있지만 **아직 인용되는 것**

| 문서 | 왜 아직 쓰나 |
|------|-------------|
| `hysteresis_analysis_260722.md` | 물리 결론(율무관 이력계, 이력 갭 46 N, τ≈0.15 s)은 여전히 유효 |
| `hys_main_curves_w40.csv` | F(I) 참고곡선 — `lib/ref_curve_w40_m2.csv` 로 복사돼 예측·안전선에 쓰인다 |
| `Stage1_loadcell_calibration_260721.md` | 포화 천장(~3234 cnt) 정보의 **유일한 출처** |

⚠ 근거 bag(84런)은 `data/` 가 gitignore 라 이미 없어졌다. 위 문서들의 수치는 재검증할 수 없다 —
`_old/traction/hys_cache/` 의 추출본이 남은 전부다.
