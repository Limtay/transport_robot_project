# TEST4 — 위치(P1~P4) × 하중(W1/W2) Current–Traction 매핑 계획 (2026-08-21)

> **목적**: 정적 견인력 `F(I)` 가 **트랙 벨트 위치**와 **하중**에 어떻게 의존하는지 판정한다.
> 이론상 하중은 독립변수여야 한다 (`F = η·Kt·N·(I−I0)/r` — 무게가 들어가지 않는다).
> **그 독립성을 실증하는 것이 이번 캠페인의 주 목적**이다.
> 선행: [test_plan_traction.md](test_plan_traction.md) Stage 3·4 / 결과 [hysteresis_analysis_260722](analysis/result/hysteresis_analysis_260722.md)
> / 로드셀 [Stage1b_loadcell_recal_260821](analysis/result/Stage1b_loadcell_recal_260821.md).

---

## 0. 판정할 질문 4개

| # | 질문 | 판정 방법 |
|---|------|-----------|
| **Q1 ★** | **하중이 F(I) 를 바꾸는가?** (이론: 안 바꿔야 함) | W1 vs W2 의 rise slope(8~13 A)·F@10 A 차이를 **제자리 반복 산포**와 비교. 차이 < 3σ 이면 독립 지지 |
| **Q2** | 위치 P1~P4 가 F(I) 를 바꾸는가? | TEST2 의 **2× 위치차**가 재현되는가. 위치간 분산 vs 제자리 분산 |
| **Q3** | 두 변수가 상호작용하는가? | 하중 효과가 위치마다 다른가 (예: 특정 위치에서만 슬립) |
| **Q4** | **이력 갭이 하중에 의존하는가?** | rise slope 가 안 변해도 갭은 변할 수 있다. 캡스턴 가설이면 **갭 ∝ 트랙 장력 ∝ 하중** — Q1 보다 예민한 지표다. 프로파일이 상행·하행을 다 담으므로 공짜로 나온다 |

> **Q4 를 넣은 이유**: 07-22 결과에서 이력 갭이 **46 N (풀스케일 26%)** 로 지배적이고, 원인이
> 캡스턴/스틱션으로 **추정만** 돼 있다. 하중 2점은 그 가설을 가르는 가장 싼 실험이다.

---

## 1. 고정 조건 (하드)

| 항목 | 값 | 근거 |
|------|-----|------|
| 모터 | **m1 단독**, `active_motors:="[1]"` | 사용자 지정 (⚠ 이전 데이터는 전부 m2 — §7.1) |
| 모드 | `auto_mode=CURRENT` | DIRECT 는 브리지 크래시 이월 버그 |
| **전류 상한** | **14.0 A (W1·W2 공통)** | 하중 독립성 판정에는 두 하중이 **같은 전류 범위**여야 한다 |
| 램프 율 | **0.375 A/s** | 상승 율의존 ~10%/5배 존재 → 전 런 고정. 07-21 `ramp_med` 와 동일 |
| 런 사이 settle | **20 s** | 종료 잔류 10~17 N → 15~20 s 이완 후 ~4.5 N |
| 로드셀 | `F[N] = (cnt − 10.08)/14.6934` | 08-21 재캘리. 검증 선형 0~200.7 N |
| **힘 abort 선** | **190 N (2802 cnt)** | 검증 상한 200.7 N 아래 10 N 마진 |
| 절대 금지 | 18 A 초과 프로파일 생성 | §6.1 |

### ⚠ 14 A 가 안전한 이유 — 그리고 왜 프로브가 필요한가

이전 실측(payload 40 kg, **m2**): 15 A → **214.6 N**(포화 위험), 14 A → **~190 N**.
14 A 는 그 근거로 정해진 값이고 **abort 선과 같다** — 마진이 없다. 게다가:

- **m1 은 처음 쓴다.** m2 곡선이 그대로 적용된다는 보장이 없다.
- **하중이 늘면 힘이 더 날 수 있다.** 이전 데이터에 "31.7 kg 에선 데드밴드 6.5 A,
  **40 kg 에선 데드밴드 없음**" — 하중이 늘수록 트랙 물림이 좋아져 같은 전류에서 힘이 더 났다.
  W2(60.3 kg)는 미지 영역이다.

⇒ **하중을 바꿀 때마다 프로브 1런을 먼저 돌린다** (§3 R0/R1). 목표 전류(14 A)는 그대로 두고,
프로브가 통과해야만 본런으로 간다. 프로브 비용은 하중당 ~1분이다.

---

## 2. 변수와 설계

| 변수 | 수준 | 비고 |
|------|------|------|
| **위치 P** | P1 / P2 / P3 / P4 | 트랙 벨트 위치 4점 |
| **하중 W** | **W1 = 39.79 kg** (18.3 + 10.46 + 11.03)<br>**W2 = 60.26 kg** (W1 + 10.25 + 10.22) | W2/W1 = **1.514×** |
| **방향** | rise / fall | 한 런이 둘 다 담는다 (분석 축) |
| 반복 | **제자리 3회** (트랙 미접촉 연속 3런) | 위상 고정 → 셀내 산포 최소(예상 CV ~1.1%) |

**4 P × 2 W × 3 rep = 24 본런** + 프로브 2 + 드리프트 확인 1 = **27런**.

제자리 반복이므로 셀내 산포는 **순수 측정 노이즈**다. 위치·하중 효과를 그 노이즈에 대고
판정한다 — 이것이 "제자리 반복"을 고른 이유다.

---

## 3. 실행 순서 (블록 · 카운터밸런스)

무게 교체는 **4회**, 위치 이동은 **3회**만 하도록 하중 순서를 위치마다 뒤집는다.

| 순서 | 블록 | 런 | 누적 |
|------|------|-----|------|
| **R0** | P1 / W1 **프로브** (0→10 A) | 1 | 1 |
| 1 | P1 / W1 ×3 | 3 | 4 |
| **R1** | P1 / W2 **프로브** (0→10 A) — 무게 추가 | 1 | 5 |
| 2 | P1 / W2 ×3 | 3 | 8 |
| 3 | **P2** / W2 ×3 — 위치 이동만 (무게 그대로) | 3 | 11 |
| 4 | P2 / W1 ×3 — 무게 제거 | 3 | 14 |
| 5 | **P3** / W1 ×3 — 위치 이동만 | 3 | 17 |
| 6 | P3 / W2 ×3 — 무게 추가 | 3 | 20 |
| 7 | **P4** / W2 ×3 — 위치 이동만 | 3 | 23 |
| 8 | P4 / W1 ×3 — 무게 제거 | 3 | 26 |
| **R2** | **P1 / W1 ×1 재측정** (세션 드리프트) | 1 | 27 |

- **카운터밸런스**: W1 이 앞선 블록 2개(P1·P3), W2 가 앞선 블록 2개(P2·P4) → 하중 효과가
  블록내 시간순서와 **혼입되지 않는다.**
- **R2 가 중요하다**: 07-22 에 날짜간 bias −2~−6 N 이 관측됐다. 세션 처음과 끝의 같은 조건
  차이가 곧 그 세션의 드리프트다. **이 값보다 작은 효과는 주장하지 않는다.**

### 라벨 규격

`t4_<P>_<W>_r<NN>` → `data/rosbags/t4_p1_w40_r01_<일시>/` (bag + profile.yaml + result.json)
프로브는 `t4_probe_<W>`, 드리프트 확인은 `t4_drift_p1_w40`.

---

## 4. 프로파일

### `t4_ramp_cycle.yaml` (본런, 87.7 s)

```yaml
name: t4_ramp_cycle
mode: current
limits: {max_current: 14.0}
motors:
  m1:
  - {type: hold, duration: 5.0,  value: 0.0}      # tare 창 (분석기가 여기서 영점)
  - {type: ramp, duration: 37.33, from: 0.0, to: 14.0}   # 0.375 A/s
  - {type: hold, duration: 3.0,  value: 14.0}     # 반전점 (이력 기억의 기준)
  - {type: ramp, duration: 37.33, from: 14.0, to: 0.0}   # 0.375 A/s
  - {type: hold, duration: 5.0,  value: 0.0}      # 잔류 관측
```

### `t4_probe.yaml` (하중 변경 직후, 66 s)

같은 구조에 정점만 **10.0 A** (`limits.max_current: 10.0`, ramp 26.67 s).

**프로브 통과 조건**: 10 A 지점 힘 `F10` 에 대해 **`F10 ≤ 105 N`**.
근거: 13.75 A 위 기울기 ~20.1 N/A → 14 A 예측 = `F10 + 80 N` ≤ 185 N < abort 190 N.
초과하면 **본런 전류를 낮춰 재계산**하고 사용자에게 보고 후 대기.

---

## 5. 안전 · abort 기준

| 조건 | 조치 |
|------|------|
| 힘 > **190 N** (ch0 > 2802 cnt) | 즉시 abort |
| 프로브 `F10 > 105 N` | 본런 중단 · 전류 재산정 · 사용자 보고 |
| `fb_velocity` ≠ 0 (홀드 중) | **트랙 슬립** — 정적 조건 위반. 기록하고 그 셀 무효 |
| 베이스라인 미복귀 > 4 N | 픽스처 밀림 — 재체결 후 재측정 |
| ch0 이 0 cnt 에 눌림 | **음방향 클리핑**(offset 10 cnt) — "힘 0" 이 아니다 |
| `rw_err` 반복 / ESTOP / FAULT | 배터리 중단 |

---

## 6. 실행 전 준비 — **해소 완료** (2026-08-21)

계획 작성 시점에 블로커 3건이 있었고 전부 처리했다. 상세 규칙은 [analysis/RULES.md](analysis/RULES.md).

| # | 문제 | 처리 |
|---|------|------|
| 6.1 | 분석기가 새 bag 을 **조용히 틀리게** 읽었다 (토픽 이름으로 매칭 → 옛 오프셋 파싱, `lc` 상수 −0.1 N / `cmd` NaN) | `analysis/lib/bagio.py` 신설. **타입으로 판별 + rosidl 역직렬화**, 안 맞으면 `BagFormatError` 로 **거부**. 실기 bag 으로 종단 검증 완료 |
| 6.2 | `data/profiles/` 통째 소실 (`.gitignore` 의 `data/`) | `analysis/profiles/` 로 이전 — **추적되는 위치**. `t4_ramp_cycle.yaml` · `t4_probe.yaml` 작성 |
| 6.3 | 러너가 없어진 `testbed_cli` 호출 | `analysis/tools/run_block.py` 신설 — `control_cli run --record` 사용, `result.json` 검증 후 이상 시 즉시 중단 |
| 6.4 | 로드셀 상수 | 08-21 값으로 갱신 완료 (`analysis/loadcell_cal.json`) |

추가로 **회귀 테스트**(`analysis/tests/test_pipeline.py`)를 넣었다. 정답을 심은 합성 bag 을
통과시켜 디코드·지표를 검증한다 — 6.1 같은 "조용히 틀리는" 실패는 이것만이 잡는다.

### ⚠ 실행 전 승인 절차

전류를 넣는 모든 런은 **미리보기 그래프 승인**을 먼저 받는다 (RULES.md §2):
`tools/profile_preview.py` 로 cmd current + 예측 힘 그래프를 만들어 확인받고,
승인된 뒤에만 `run_block.py` 로 실행한다.

## 7. 알려진 교란요인

### 7.1 ⚠ m1 은 이전 데이터와 다른 모터다

07-21/22 의 84런은 **전부 m2** 다. 따라서:

- **예측·안전선 산출에는** m2 곡선을 쓴다 (같은 기종·같은 벤치이므로 근사로 유효).
- **결론 비교에는 쓰지 않는다.** m1 과 m2 의 차이는 이번 데이터로 분리할 수 없다
  (모터 × 날짜 혼입). W1 셀이 07-22 와 다르게 나와도 그것이 모터차인지 날짜차인지
  **알 수 없다** — 그렇게 해석하지 말 것.

### 7.2 스프로킷 위상은 셀 간 미제어

제자리 반복이라 **셀 안에서는 고정**, 셀 사이(위치 이동·무게 교체 시)에는 바뀐다.
`fb_position` 이 200 Hz 로 기록되므로 **사후 위상 라벨링이 가능하다** — 위치 효과가
크게 나오면 그 축으로 다시 갈라 본다 (원 계획 Stage 3 의 미해결 과제).

### 7.3 크리프

정지 직후 힘이 15~20 s 에 걸쳐 이완한다. settle 20 s 는 그 시정수 기준이고,
상승 기울기의 율의존(~10%)도 크리프 기인으로 **추정만** 돼 있다. 율을 고정했으므로
셀 간 비교에는 영향이 없지만, **절대 slope 를 이론값과 대조할 때는 감안**한다.

---

## 8. 분석 계획

1. 런별: tare(첫 5 s) → rise/fall 분리 → `F(I)` 곡선, slope(8~13 A), 데드밴드 `I0`,
   14 A 피크, 이력 갭(중간 전류대), 베이스라인 복귀.
2. 셀별(P,W): 3반복의 평균 ± σ → **제자리 산포 = 판정 기준선**.
3. **Q1**: `ΔF_W = F_W2 − F_W1` 을 전류축 전체에서. 3σ 밴드와 겹치면 독립 지지.
4. **Q2**: 위치 4수준의 분산 vs 제자리 분산. TEST2 의 2× 차이 재현 여부.
5. **Q3**: (P,W) 셀 평균의 상호작용 항.
6. **Q4**: 이력 갭 vs 하중 — 캡스턴 가설이면 **갭이 1.5× 로 커져야** 한다.
7. 산출: `F(I; P, W)` 맵 + 불확도표 → MPC 입력. 결과 문서는 `analysis/result/`.

---

## 9. 규모 · 시간

| 항목 | 시간 |
|------|------|
| 본런 24 × (87.7 s + settle 20 s) | 43 분 |
| 프로브 2 × (66 s + 20 s) | 3 분 |
| 드리프트 확인 1 | 2 분 |
| 위치 이동 3회 | 사용자 소요 |
| 무게 교체 4회 | 사용자 소요 |
| **순수 런타임 합계** | **~48 분** |

무인 구간은 블록 단위(3런 = ~5.4분)다. 블록 사이마다 사용자 개입(위치/무게)이 필요하므로
`run_campaign.py --repeats 3` 을 블록마다 호출하는 형태가 맞다.
