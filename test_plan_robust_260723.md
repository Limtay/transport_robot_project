# TEST3 Stage 3 — 트랙 위치 외란·시간 의존성 → 제어 강건성 가이드 (2026-07-23 설계)

> **목표가 바뀌었다**: Stage 2 에서 F(t)=Preisach[I] 모델은 확보됨(`analysis/result/hysteresis_analysis_260722.md`).
> 이번 Stage 는 모델을 더 정밀하게 만드는 게 아니라, **모델이 통제할 수 없는 외란의 크기를 재는 것**이다.
> 산출물 = "제어기가 견뎌야 할 불확실성 밴드" 정량표 → **제어기 강건성 설계 가이드**.

## 1. 배경·가설 (사용자 물리 지식 반영)

- 고무 트랙은 시간이 지나면 **특정 형태로 굳는다**(parking set). 어느 트랙 지점이 견인력을 받느냐에 따라
  F(I) 특성이 달라진다. 이 지점 특성은 회전·발열·온도에 따라 **지속적으로 변하므로 제어 변수로 쓸 수 없고
  외란(noise/disturbance)으로 취급**해야 한다.
- Stage 2 관측과 부합: 날짜간 bias −2~−6N, 세트내 곡선산포 1.4~3.5N, 크리프 1~4N/8s — 즉
  **F 는 I 와 시간(Time)에 의존**: Time = ①크리프(초 스케일) ②세션 내 열드리프트(분 스케일)
  ③트랙 상태 변화·parking set(일 스케일).
- **크리프의 지위 (2026-07-23 사용자 결정)**: ①크리프는 외란이 아니라 **모델 변수**다 — 하강 진입 등
  반전 직후 I 고정에도 F 가 초 단위로 계속 변하며(종료 후 0A 잔류 ~10N 이 15~20s 이완하는 관측 포함)
  제어 오차에 직접 들어온다. 목표 모델을 다음으로 확장:
  **F(t) = Preisach[I(t)] + Σ_j z_j(t)**, ż_j = (−z_j + b_j·ΔH)/τ_j (크리프 상태, ΔH = 이력출력 변화,
  τ_j ∈ {0.5, 2, 8, 30}s 로그격자 + 기확인 빠른지연 0.15s). b_j 는 hold 구간 **선형회귀**로 추정
  (Hammerstein 구조 유지 — 러닝 불필요 가설 계속). ②③은 여전히 외란(강건성 마진)으로 취급.
- **분해할 분산 성분**: σ²(트랙위치) + σ²(세션내 시간/열) + σ²(날짜) + σ²(반복 바닥). 각각의 크기와
  시간 스케일이 곧 제어기 요구사항이다 (예: 위치 분산이 지배적·빠름 → 피드백 대역/외란 관측기,
  날짜 드리프트가 지배적·느림 → 적분항/적응 보정).

## 2. 세션 구조 (~30분, 트랙 위치 5개 블록)

트랙 둘레에 위치 표식 **P1~P5** (사용자가 사전에 표시). 한 세션 = 5블록, 블록마다 사용자가 트랙을
돌려 위치 세팅 후 선언 → 세션이 해당 블록 배터리 실행. **모든 블록은 동일 프로브 3런** (위치간 비교가
성립하려면 프로브가 완전히 같아야 함).

| 블록 내 순서 | 프로파일 | 길이 | 역할 |
|---|---|---|---|
| 1 | `rb_forc14` | 41s | **기준 이력 프로브** — rise/fall 곡선·기울기·이력갭 (P4 기준런 겸용, 구 30분 배터리의 핵심 축약) |
| 2 | `rb_creepA` (홀수일) / `rb_creepB` (짝수일) | ~115s | **크리프 모델 ID 프로브** — 다레벨·양측·0A 이완 hold (일내 동일 → 위치 분산분해 성립, 격일 교대 → 레벨 커버리지 확보) |
| 3 | `rb_ctrl_s<k>` | ~90s | **V1 제어형 검증** — slew≤1A/s 무작위 궤적(블록 k 마다 고정 seed k, hold 1~8s 포함), frozen 모델 예측오차 + 크리프 항 검증 |

- 블록 소요: 모터 250s + settle 15s×2 + 오버헤드 ≈ **5.1분** → 5블록 = 25.5분 + 회전 5회 ≈ **28~30분**.
- 전류 상한 14A·payload 40kg·m2 — Stage 2 와 동일 (비교 연속성).
- ctrl seed 는 **블록번호로 고정(1~5), 날짜 불변** — 같은 궤적의 반복측정이 분산분해에 유리. (모델 학습에
  안 쓴 파형이므로 '미학습 검증' 자격은 이미 충족.)

### 세션 프로토콜 (확립된 문답 방식)

```
[세션 시작 1회] 브리지 브링업(INIT→IDLE) + latency_timer=1 확인 + payload 40kg 확인
[블록 k = 1..5]
  사용자: 트랙을 위치 Pk 로 회전(모터 IDLE·0A 상태에서) → "위치 k" 선언
        (+ 첫 블록 전: 직전 정차 위치·정차 시간, 가능하면 실온도 알려주면 메타데이터로 기록)
  세션:  run_battery.sh rb_block<k>.json --suffix p<k>  실행 → 완료 알림 대기 → 한 줄 보고
[세션 종료] 세션 JSON 확인만 (분석은 다일 수집 후 일괄)
```

### 다일(多日) 설계

- **≥5일 반복** 권장. 날짜마다 **블록 방문 순서를 바꾼다** (열드리프트와 위치효과의 교락 해소 — 세션 내
  시간이 갈수록 모터가 뜨거워지므로, 매일 같은 순서면 위치효과에 열효과가 섞인다):
  - D1: 1→2→3→4→5 / D2: 3→5→2→4→1 / D3: 5→4→1→2→3 / D4: 2→1→4→3→5 / D5: 4→3→5→1→2 (라틴방진)
- 세션 메타데이터(사용자 구두 → 세션 JSON `note`): 직전 정차 위치·정차 시간(parking set 가설 검정용),
  실온(대략), payload 확인.
- 07-21/07-22 기존 84런은 위치 미기록 — "기준일(위치 무통제)" 데이터로만 사용.

## 3. 프로파일 스펙 (Opus 스크립트 작성용 — 정확 사양)

전 프로파일 공통: `limits: {max_current: 14.0, slew_rate: 1.1}` (파서가 slew 위반 자체 검증),
motors: m2, 세그 경계 연속(린터 seg_jump ≤0.01A), `lint_profiles.py 2 14.0` 통과 필수.

**`rb_forc14.yaml`** (41s) — Stage 2 `hys_forc_rev14` 와 동일 (연속성 위해 재사용 가능):
```
hold 5s @0 → ramp 0→14 @1.0A/s (14s) → hold 3s @14 → ramp 14→0 @1.0A/s (14s) → hold 5s @0
```

**`rb_creepA.yaml`** (홀수일, ~112s) — 레벨 세트 A {rise12, fall6, relax0}:
```
hold 3s @0 → ramp 0→12 @1.0 (12s) → hold 30s @12   ← 상승측 크리프 @12A
→ ramp 12→6 @1.0 (6s) → hold 30s @6                ← 하강측 크리프 @6A
→ ramp 6→0 @1.0 (6s) → hold 25s @0                 ← 0A 이완 (종료 잔류의 시정수)
```

**`rb_creepB.yaml`** (짝수일, ~119s) — 레벨 세트 B {rise8, fall10, relax0}:
```
hold 3s @0 → ramp 0→8 @1.0 (8s) → hold 30s @8      ← 상승측 크리프 @8A
→ ramp 8→14 @1.0 (6s) → hold 3s @14 → ramp 14→10 @1.0 (4s) → hold 30s @10   ← 하강측 크리프 @10A
→ ramp 10→0 @1.0 (10s) → hold 25s @0               ← 0A 이완
```
(부수 효과: 두 프로파일 모두 마이너루프 프로브 겸용 — 이력갭·반환점 지표 산출.
τ 식별성: hold 30s 는 τ={0.5,2,8}s 를 직접, τ=30s 는 0A 이완 25s + 세션 반복으로 부분 식별 —
필요 시 후속 단일 세션에서 120s hold 1회 보강.)

**`rb_ctrl_s1..s5.yaml`** (~90s, 생성기 `gen_robust.py` 로 시드 고정 생성):
- 시드 k 의 RNG 로: 목표전류 U[1,13]A (직전 목표와 |Δ|≥1.5A 보장), 램프 속도 U[0.4,1.0]A/s,
  램프 후 hold U[1,8]s (**긴 hold 포함 — 크리프 항의 in-context 검증용**) — 누적 ~84s 까지 반복
  → 마지막 ramp→0 @1.0 + hold 4s (이완 꼬리). 시작은 hold 2s @0.
- ramp/hold 세그 체인으로 YAML 출력 (custom 불필요 — 파서 네이티브 세그로 재현성·가독성 확보).
- 반전(방향 전환) 횟수 ≥8회/런 이 되도록 목표 개수 조정 (이력 활성화 검증 목적).

**매니페스트** `rb_block1..5.json`: 각각 runs = [rb_forc14, rb_creepminor, rb_ctrl_s<k>] ×1,
settle_s=15, block_cooldown 불필요(블록=1매니페스트), label=`rb_block<k>`.

## 4. 오케스트레이터 수정 사양 (Opus 작업)

`analysis/traction/run_battery.py` 에 추가:
1. **`--suffix <str>`** — 런 이름·세션 라벨에 삽입 (예: `rb_forc14_p3_r01`, `battery_rb_block3_p3_<ts>.json`).
   위치가 폴더명·세션 JSON 에 영구 기록되는 유일한 경로 — 필수.
2. **`--note "<자유텍스트>"`** — 세션 JSON 에 저장 (정차 이력·실온 등 구두 메타데이터).
3. 기존 안전장치(프리플라이트 IDLE / result.json 검증 / 런후 상태확인 / 이상 시 abort) 그대로.

`run_battery.sh` 도 인자 패스스루 (`run_battery.sh <manifest> [--suffix ..] [--note ..]`).

## 5. 분석 사양 (다일 수집 후 일괄 — `rb_analysis.py` 신규)

### 런당 지표 (CSV 한 행 = 런 하나: date, day_order, block_order, position, profile, …)
| 지표 | 출처 런 | 의미 |
|---|---|---|
| rise_slope(8–13A), gap@7A, F@14hold | rb_forc14 | 이력 골격의 위치/일간 변동 |
| creep ΔF(30s)·다지수 피팅 {b_j} @각 hold 레벨·측 + 0A 이완 τ | rb_creepA/B | **크리프 모델 계수** + 위치/일간 변동 |
| minor 반환오차, 이력갭 | rb_creepA/B | 이력 구조 안정성 |
| **ctrl_RMSE, ctrl_bias** (frozen 모델 예측 대비, 크리프항 유/무 2종) | rb_ctrl | **제어기가 보게 될 실제 모델오차 + 크리프항의 개선 기여** |
| baseline 복귀 잔류 | 전 런 | 픽스처/트랙 상태 플래그 |

### 크리프 모델 동정 (§1 확장모델의 {b_j, τ_j} 추정)
- 전 세션·전 블록의 hold 구간(30s×2 + 0A 25s + ctrl 내 hold 1~8s)을 모아 τ 격자 {0.5,2,8,30}s 고정 후
  b_j 를 **선형회귀** (레벨·측 의존성: b_j 가 ΔH 에 비례하는지, 별도 항이 필요한지 검정).
- 검증: ①ctrl 런에서 크리프항 추가 시 RMSE 개선폭 ②0A 이완 예측(15~20s 관측 재현) ③rise-rate 의존
  (~10%/5배, Stage 2 미해결)이 크리프항으로 설명되는지 — camp_ramp_slow/med/fast 재예측.
- 산출: `preisach_creep_model.npz` (Preisach 가중치 + {b_j,τ_j}) — 제어기 피드포워드용 최종 모델.

### 분산 분해 (핵심 산출)
- 각 지표에 대해 **성분별 분산**: 위치간(같은 날 5블록) / 세션내 순서(열드리프트, 라틴방진으로 분리) /
  날짜간 / 잔차(반복 바닥, 07-22 의 1.6% CV 와 대조). 시각화: 지표 × (위치, 날짜) 매트릭스 히트맵.
- parking set 검정: 첫 블록(정차 직후 위치)의 지표가 나머지와 계통적으로 다른가 + `note` 의 정차이력 대조.

### 최종 산출물 — 제어 강건성 가이드 (`analysis/result/robust_guide_<date>.md`)
1. **불확실성 밴드**: I 구간별 |F_실측 − F_모델| 의 분포 (P50/P95) — "제어기는 X N (P95) 의 모델오차를
   외란으로 견뎌야 한다".
2. **시간 스케일 표**: 크리프 τ(초) / 세션내 드리프트(분당 N) / 날짜간 점프(N) — 각각에 대응하는
   제어 수단 권고(피드백 대역·적분항·적응/리셋 보정).
3. 위치 외란의 백색성 판단: 위치별 편차가 무작위인지(→ 확률적 외란 마진) 특정 위치가 재현되게
   튀는지(→ 트랙 마킹·회피 불가, 마진 상향).

## 6. 안전·제약 (Stage 2 와 동일 + 추가)

- ≤14A / `active_motors:="[2]"` / CURRENT 만 / latency_timer=1 / HW_ESTOP 사용자 담당.
- **트랙 회전은 반드시 IDLE(0A) 상태에서** — 블록 사이가 자연 IDLE 이므로 그때만 회전.
- 프로파일 slew_rate 1.1 파서 검증 + 린트 통과 후 실행. 미검증 프로파일 즉석 투입 금지.

## 7. 실행 로드맵

| 단계 | 담당 | 내용 |
|---|---|---|
| ① 구현 | **Opus** | §3 프로파일 생성기 + §4 오케스트레이터 수정 + 린트/드라이런 검증 + 블록 매니페스트 5개 |
| ② 수집 | Sonnet ×≥5일 | §2 프로토콜 (사용자 위치 선언 → 블록 실행, 문답 최소) |
| ③ 분석 | Fable | §5 분산분해 + 강건성 가이드 작성 → 사용자 검토 |
